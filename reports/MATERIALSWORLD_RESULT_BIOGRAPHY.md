# MaterialsWorld — Result Biography v0.1

## Current result

```text
Primary organizational result:
    CEOS Structural Representation Families v0.2

Partition Admission:
    ADMITTED_WITH_SCOPE

Release State:
    RELEASED as methodological pilot

Literature Cross Mapping:
    WITHHELD

Physical mechanism claims:
    WITHHELD

Governance v0.2.1:
    QUALIFIED, not ADMITTED
```

## Biography graph — critical turning points

### BIO-001 — Blind protocol created

```text
Event:
    REPRESENTATION_CREATED

Timing:
    PRE_RESULT

Artifacts:
    FORBIDDEN.md
    config/project.yaml
    raw/MANIFEST.template.json
```

Material names and published classes were prohibited as analytical inputs before data download.

### BIO-002 — Pilot WORLD registered

```text
Event:
    DATASET_REGISTERED

Objects:
    3,000

Sampling:
    deterministic stratified cluster windows

API version:
    0.87.2.dev4+g6bd8ed856
```

The sampling limitation was preserved: family fractions are not population estimates.

### BIO-003 — Representation-object ontology emerged

Observed distinctions:

```text
composition  → MAP
symmetry     → RECORD
structure    → HIERARCHICAL COMPOSITE
has_props    → RECORD + MAP VIEW
origins      → LIST[RECORD]
```

`structure → recursive TREE` remained unresolved and was not admitted.

### BIO-004 — Initial organizational-observable vectors

The first vectorization produced 163 candidate observables and retained 110.

### BIO-005 — Observer Audit contradicted the first vectorization

```text
World disagreement:
    duplicated size observables
    missingness encoded by has_props

Response:
    Representation revised from v0.1 to v0.2

Timing:
    before clustering
```

Changes:

- missingness separated into `availability_state`;
- binary complements removed;
- within-block correlations ≥0.995 removed;
- `composition.total` excluded as duplicated size encoding.

### BIO-006 — Frozen structural partition admitted

```text
Families:
    4

Bootstrap ARI:
    0.9796

ORGANIZATION vs OBSERVER_AUGMENTED:
    ARI 0.9945

All-column Null gap:
    +0.0755
```

### BIO-007 — Structure-dominated claim tested

```text
Remove composition:
    ARI 0.9871

Remove symmetry:
    ARI 0.9814

Remove structure:
    ARI 0.0775
```

Admitted wording:

> Within the selected representation, the structure block is required to recover the current partition.

No mechanism claim was admitted.

### BIO-008 — First out-of-sample projection rejected

```text
Failure:
    preprocessing median was refitted after adding examples

Replay error:
    raw 3.15×10⁻⁴
```

The classification was not accepted.

### BIO-009 — Frozen preprocessing restored

Training medians and scales were frozen.

```text
Raw replay error:
    0.0

Scaled replay error:
    0.0
```

Only after exact replay were FeS, NaCl, SiO₂ and SrTiO₃ assignments accepted.

### BIO-010 — Builder Admission initially failed

Key-order and serialization audits produced errors near `10⁻¹⁵`.

```text
World disagreement:
    floating-point accumulation depended on serialization order

Response:
    canonical sorting before aggregation
```

All downstream observables, partitions and examples were recomputed.

Final result:

```text
field permutation       0.0
nested permutation      0.0
key-order permutation   0.0
serialization replay    0.0
deterministic rerun     0.0
```

### BIO-011 — OOD and Boundary separated

Blind stress selection produced:

```text
B10H14C10S(OF2)3
    Admission State: OOD
    Nearest family: F-002
    Admissible family: WITHHELD

LiMnVF6
    Admission State: BOUNDARY
    Nearest family: F-001
    Admissible family: WITHHELD
```

### BIO-012 — Scale-driven OOD hypothesis withheld

For B₁₀H₁₄C₁₀S(OF₂)₃:

- one scale Null removed OOD;
- structure-unit normalization did not;
- composition independently remained OOD.

Admitted wording:

```text
scale-sensitive
not admitted as scale-driven
```

### BIO-013 — Conflict Topology introduced

LiMnVF₆ showed:

```text
{Global, Structure}
{Local, Composition, Symmetry}
{Observer}
```

The first population run was rejected because Local geometry was computed in the wrong 105-observable scope instead of the frozen 75-observable ORGANIZATION view.

### BIO-014 — Conflict Topology v0.3 qualified

After scope correction and tie-complete deterministic kNN:

```text
low-margin objects:       52
low-margin global OOD:     4
Boundary objects:         48
Relational Boundary:       8
odds ratio:            30.87
p:                   4.14e-09
```

Supported claim:

```text
RELATIONAL_BOUNDARY is a population-supported
candidate organizational subtype in this pilot.
```

No physical phase-boundary claim was made.

### BIO-015 — Claim Type System v0.1 challenged

Governance Red Team registered 16 adversarial scenarios.

```text
Controls:
    2/2

Specification gaps / unprotected transitions:
    16/16
```

The result was not interpreted as failure of the material analysis. It triggered governance reassessment.

### BIO-016 — Governance v0.2 remediation

Introduced:

- lifecycle State Machine;
- content-addressed EvidenceRef and ProvenanceRef;
- identity registry;
- event-sourced maturity;
- acyclic Claim DAG;
- controlled Interpretation Boundary;
- artifact registry independent of file extension.

The same 16 attacks were rejected.

Status:

```text
QUALIFIED
not ADMITTED
```

### BIO-017 — A new unregistered pipeline gap appeared

A failed document lint was masked by a later successful shell command.

```text
Diagnostic:
    GOV-PIPE-001
```

Governance v0.2.1 added a fail-fast integrity entrypoint:

```bash
set -euo pipefail
```

Registered attacks after patch:

```text
17/17 rejected
```

This event demonstrates why “remaining registered gaps = 0” is not equivalent to “no unknown gaps exist”.

### BIO-018 — Recommendations released

Development proposals were prepared for:

- Materials Project Representation Passport;
- nested Object Signatures;
- Representation Ecology;
- Capability Passport;
- Builder transparency and replay;
- scientific Result Passports;
- Mapping Stability;
- Interpretation Boundary.

---

# Hypothesis biography

| Claim | State | Reason |
|---|---|---|
| Current partition is structure-dominated | ADMITTED_WITH_SCOPE | structure ablation destroys recovery |
| B₁₀… OOD is entirely scale-driven | WITHHELD / NOT_ADMITTED | registered Scale Nulls disagree |
| Every Boundary is relational | REJECTED | total Boundary rate remains inside Conflict Null interval |
| RELATIONAL_BOUNDARY exists as a subtype | QUALIFIED_WITH_SCOPE | enrichment exceeds Packet Null |
| Governance v0.1 is proof-carrying | REASSESSMENT_REQUIRED | 16 specification gaps exposed |
| Governance v0.2.1 closes all possible gaps | WITHHELD | only 17 registered attacks tested |
| Classical physical mapping is established | WITHHELD | literature gate remains locked |
| Physical mechanism is established | WITHHELD | no mechanism Admission protocol |

---

# WITHHELD register

## Literature Cross Mapping

```text
Release State:
    WITHHELD

Reason:
    frozen organizational entities exist,
    but literature mapping has not been independently qualified

Pending gates:
    Mapping Provenance
    Mapping Stability
    counterexample search
    Cross Mapping Asymmetry audit
```

## Physical mechanism claims

```text
Release State:
    WITHHELD

Reason:
    organizational geometry does not transfer mechanism evidence

Pending gates:
    independent physical evidence
    mechanism-specific Nulls
    Interpretation Admission
```

## Governance Admission

```text
Release State:
    WITHHELD

Current state:
    QUALIFIED

Pending gates:
    independent Red Team
    unregistered mutation attacks
    corrupted-registry tests
    real claim-graph audit
```

---

# What the biography changes

The final family result is no longer presented as an isolated endpoint. Its credibility can be evaluated together with:

- rejected preprocessing;
- failed replay;
- Builder canonicalization;
- representation revision;
- withheld scale explanation;
- rejected Conflict scope;
- governance specification gaps;
- later remediation and regression.

These are not editorial details. They are events that changed the type, scope or admissibility of the claim.

---

# Interpretation Boundary

## Supports

- the documented lifecycle of the MaterialsWorld pilot claims and the listed evidence-linked turning points;
- distinction between rejected, withheld, admitted and reassessment states;
- reproducibility analysis of major representation and governance changes.

## Does not support

- completeness of every exploratory action ever performed;
- a claim that the final organizational families are physically fundamental;
- physical mechanism;
- thermodynamic phase;
- microscopic Hamiltonian;
- causal explanation;
- prediction of untested properties;
- evidence inheritance through classical vocabulary.

## Cross Mapping Asymmetry

> **Vocabulary correspondence ≠ evidence inheritance.**

<!-- CEOS-INTERPRETATION-BOUNDARY v0.1 -->
## Interpretation Boundary — Document Scope

### Supports

- the neutral organizational partition, Relation Pattern, Null comparison, or population-support claim explicitly reported for the analyzed sample and registered methodology.

### Does not support

- physical mechanism;
- thermodynamic phase;
- microscopic Hamiltonian;
- causal explanation;
- prediction of untested properties;
- transfer of evidence through vocabulary or Cross Mapping;
- universal validity outside the registered WORLD, sample, protocol, and version;
- identity with a classical physical category without separately admitted post hoc Cross Mapping.

### Cross Mapping Asymmetry

> **Vocabulary correspondence ≠ evidence inheritance.**
